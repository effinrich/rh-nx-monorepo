# PostGreSQL

_[PostgreSQL](https://www.postgresql.org/about/)_ is an entrprise class, open-source relational database that supports both SQL (relational) and JSON (non-relational) querying.

The Platform data infrastructure supports PosgreSQL as an option for a relational database.
