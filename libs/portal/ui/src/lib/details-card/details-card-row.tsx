import { ListCardRow, ListCardRowProps } from '../list-card/list-card-row'

const DetailsCardRow = (props: ListCardRowProps) => {
  return <ListCardRow px={0} {...props} />
}

export default DetailsCardRow
