// In Chakra v3, `styled` was removed from @chakra-ui/react.
// Provide a `styled` helper based on the Chakra factory instead of @emotion/styled.
import { chakra } from '@chakra-ui/react'

export const styled = chakra
