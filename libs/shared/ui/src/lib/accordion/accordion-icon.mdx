---
title: AccordionIcon
description: Documentation for the AccordionIcon component
---

import { AccordionIcon } from '@/components/AccordionIcon'
import { Canvas, Meta, Story, Controls, ArgTypes } from 'storybook/blocks'
import * as AccordionIconStories from './accordion-icon.stories'

<Meta of={AccordionIconStories} />

# AccordionIcon

The `AccordionIcon` component is a reusable UI element.

## Import

```tsx
import { AccordionIcon } from '@/components/AccordionIcon'
```

## Usage

### Basic Example

<Canvas of={AccordionIconStories.Default} />

```tsx
<AccordionIcon>
  Content
</AccordionIcon>
```

## Props

<ArgTypes of={AccordionIcon} />

This component accepts standard HTML attributes.

## Accessibility

The `AccordionIcon` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
import { Stack } from '@chakra-ui/react'

<Stack spacing={4}>
  <AccordionIcon>
    First item
  </AccordionIcon>
  <AccordionIcon>
    Second item
  </AccordionIcon>
</Stack>
```

## Responsive Design

Use responsive props for different screen sizes:

```tsx
<AccordionIcon
  
  width={{ base: '100%', md: 'auto' }}
>
  Responsive AccordionIcon
</AccordionIcon>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- [Box](/components/box) - Base layout component
- [Stack](/components/stack) - Layout with spacing
- [Flex](/components/flex) - Flexbox layout
